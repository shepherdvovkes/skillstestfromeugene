# Production Environment Configuration Template
# Copy this file to .env.production and update with your production values
# DO NOT COMMIT .env.production TO VERSION CONTROL

# =============================================================================
# REQUIRED PRODUCTION CONFIGURATION
# =============================================================================

# WalletConnect Project ID (REQUIRED)
# Get your project ID from https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your-production-project-id-here

# =============================================================================
# PRODUCTION RPC URLS (Recommended to use private endpoints)
# =============================================================================

# Polygon Network RPC URL
NEXT_PUBLIC_POLYGON_RPC_URL=https://your-polygon-rpc-endpoint.com

# Linea Network RPC URL  
NEXT_PUBLIC_LINEA_RPC_URL=https://your-linea-rpc-endpoint.com

# BSC Network RPC URL
NEXT_PUBLIC_BSC_RPC_URL=https://your-bsc-rpc-endpoint.com

# =============================================================================
# PRODUCTION FEATURE FLAGS
# =============================================================================

# Enable auto-reconnect functionality
ENABLE_AUTO_RECONNECT=true

# Enable connection health checks
ENABLE_HEALTH_CHECKS=true

# Enable logging (disabled in production by default)
ENABLE_LOGGING=false

# =============================================================================
# PRODUCTION SERVER CONFIGURATION
# =============================================================================

# Server port
PORT=443

# Server hostname
HOSTNAME=your-domain.com

# =============================================================================
# PRODUCTION SECRETS (GENERATE NEW ONES FOR PRODUCTION)
# =============================================================================

# JWT Secret for API authentication (32 character random string)
JWT_SECRET=generate-new-32-character-secret-here

# Session Secret for secure sessions (32 character random string)
SESSION_SECRET=generate-new-32-character-secret-here

# API Key for internal services (UUID v4)
INTERNAL_API_KEY=generate-new-uuid-v4-here

# Database Connection Secret (32 character random string)
DB_SECRET=generate-new-32-character-secret-here

# Encryption Key for sensitive data (32 character random string)
ENCRYPTION_KEY=generate-new-32-character-secret-here

# =============================================================================
# PRODUCTION SECURITY CONFIGURATION
# =============================================================================

# Rate limiting - requests per minute per IP
RATE_LIMIT_REQUESTS_PER_MINUTE=50

# Rate limiting - requests per minute per wallet
WALLET_RATE_LIMIT_REQUESTS_PER_MINUTE=20

# Session timeout in seconds
SESSION_TIMEOUT=1800

# CSRF token expiration in seconds
CSRF_TOKEN_EXPIRATION=900

# =============================================================================
# PRODUCTION NOTES
# =============================================================================

# 1. SECURITY REQUIREMENTS:
#    - Generate new secrets for each production environment
#    - Use private RPC endpoints for better performance and security
#    - Set ENABLE_LOGGING=false in production
#    - Use HTTPS only in production
#    - Implement proper monitoring and alerting
#
# 2. SECRET GENERATION:
#    - Use cryptographically secure random generators
#    - Rotate secrets regularly (every 90 days recommended)
#    - Store secrets securely (use secret management services)
#    - Never commit secrets to version control
#
# 3. MONITORING:
#    - Set up logging aggregation
#    - Monitor rate limiting and security events
#    - Set up alerts for suspicious activity
#    - Regular security audits and penetration testing
